# 安全策略

CatShell 是一个 SSH 运维客户端，它接触的内容天然敏感：服务器地址、登录凭据、私钥、主机指纹。
本文档说明我们如何保护这些数据、哪些边界不允许被突破，以及如何报告安全问题。

## 支持版本

| 版本 | 安全更新 |
| --- | --- |
| 0.1.x（当前主线） | ✅ 支持 |
| 更早版本 | ❌ 不支持 |

安全修复只会合并到当前主线并随下一个补丁版本发布。

## 报告安全问题

**请不要通过公开 issue 报告安全漏洞。**

优先使用 GitHub 的私有漏洞报告通道：
<https://github.com/smcloudcat/CatShell/security/advisories/new>

如果无法使用该通道，可提交一个不含技术细节的 issue 说明"存在安全问题，需要私下沟通渠道"，我们会与你建立联系。

报告中请尽量包含：

- 受影响的版本号或 commit
- 复现步骤或最小 PoC
- 影响评估（可导致凭据泄露、绕过主机校验、任意命令执行等）
- 你希望在披露公告中使用的署名方式

我们会在 7 天内确认收到，并在修复版本发布后公开致谢（除非你希望匿名）。

## 安全设计边界

以下规则属于项目硬约束，任何 PR 都不得放宽：

- **凭据不入库。** 连接密码与私钥口令只用于当前连接进程，绝不写入主机配置 Store、localStorage、日志或导出 JSON。
- **导出必剔除敏感字段。** 配置导出与备份在写盘前显式删除 `password` 和 `passphrase`。
- **主机指纹严格校验（TOFU）。** 首次连接必须由用户人工确认 SHA-256 指纹并写入 known_hosts；已记录指纹必须严格匹配；指纹发生变化时直接阻断连接，不提供任何绕过入口。
- **指纹确认有超时。** 等待用户确认的请求有超时上限，不会无限挂起。
- **保险箱主密码不落盘。** 凭据保险箱使用 PBKDF2 派生密钥 + AES-GCM 加密；锁定或退出时清除内存中的明文凭据。
- **转发只绑定回环。** 本地 / 远程 / SOCKS5 转发的绑定地址限定为 `127.0.0.1`、`localhost`、`::1`，不允许暴露到 `0.0.0.0`。
- **远程命令不拼接用户输入。** 监控采集使用固定的只读命令；Ping / Traceroute 目标、进程 Kill 的 PID 与信号均经过白名单或格式校验后才进入远程 shell。
- **权限最小化。** Tauri capability 按需声明，不为了调试方便放宽。

## 用户侧建议

- 优先使用 SSH 私钥或 SSH Agent 认证，减少密码在网络中的使用。
- 私钥建议设置口令，并交由系统 Agent 管理。
- 首次连接时认真核对服务器指纹，不要在没有其他渠道验证的情况下直接确认。
- 为凭据保险箱设置一个独立于其他用途的主密码。
- 保持应用为最新版本，以便获得安全修复。

## 已知限制

- 应用当前只面向 Windows，安全测试也主要在 Windows 上进行。
- 应用不提供自建服务端组件；更新依赖 GitHub Releases，端点被劫持时无法通过应用内机制检测。
- 审计日志为本地明文存储，其完整性依赖本机账户安全，不做防篡改保证。
